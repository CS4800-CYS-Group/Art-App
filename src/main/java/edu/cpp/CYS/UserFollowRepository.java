/*package edu.cpp.CYS;

import java.util.List;

import org.springframework.data.jpa.repository.JpaRepository;

public interface UserFollowRepository extends JpaRepository<UserFollow, Long> {
   List<UserFollow> findByFollower(U follower);
   List<UserFollow> findByFollowing(U following);
   UserFollow findByFollowerAndFollowing(U follower, U following);
}*/
